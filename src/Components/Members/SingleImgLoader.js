import React from 'react';
import Skeleton from '@mui/material/Skeleton';

const SingleImgLoader = () => {
  return <Skeleton variant="circular" width={50} height={50} />;
};

export default SingleImgLoader;
