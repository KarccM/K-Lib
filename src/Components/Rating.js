import React from 'react';
import classes from '../main.module.css';

const Rating = () => {
  return (
    <div className={`${classes.members} ${classes.Rating}  basis-1/3`}>
      Rating 6.5/10
    </div>
  );
};

export default Rating;
